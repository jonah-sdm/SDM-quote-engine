{
  "name": "sdm-quote-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram bot that generates sample trade quotes for the SDM sales team.",
  "scripts": {
    "telegram:set-webhook": "node scripts/set-telegram-webhook.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.37.0",
    "dotenv": "^17.3.1"
  },
  "engines": {
    "node": ">=18"
  }
}
